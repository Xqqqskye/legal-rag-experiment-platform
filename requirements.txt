# Python 3.11 recommended. Install a CUDA-compatible torch wheel separately
# when using a GPU; the normal PyPI build works for the CPU sample.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.8,<3.0
python-dotenv>=1.0,<2.0
elasticsearch>=8.12,<9.0
openai>=1.50,<3.0
httpx>=0.27,<1.0
numpy>=1.26,<3.0
sentence-transformers>=3.0,<6.0
